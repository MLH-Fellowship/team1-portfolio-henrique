limit_req_zone $binary_remote_addr zone=timeline_limit:10m rate=1r/m;

server {
    listen 80;
    server_name henrique-portfolio.duckdns.org;

    if ($host = henrique-portfolio.duckdns.org) {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name henrique-portfolio.duckdns.org;

    location /api/timeline_post {
        limit_req zone=timeline_limit;
        limit_req_status 429;
        proxy_pass http://myportfolio:5000/;
    }

    location / {
        proxy_pass http://myportfolio:5000/;
    }

    # Load the certificate files.
    ssl_certificate /etc/letsencrypt/live/henrique-portfolio.duckdns.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/henrique-portfolio.duckdns.org/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/henrique-portfolio.duckdns.org/chain.pem;
}
